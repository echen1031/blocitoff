<div class="page-header">
  <h1><%= @list.description %></h1>
</div>
<% if policy(@list).edit? %>
  <%= link_to "Edit", edit_list_path(@list), class: 'btn btn-sucess' %>
<% end %>
<div class="col-md-4">
  <ul class="list-group">
    <li class="list-group-item">
      <span class="badge">14 days left</span>
      <%= @list_item.content %>
    </li>
  </ul>
</div>
